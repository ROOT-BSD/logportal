# Забороняємо прямий доступ до чутливих файлів
<FilesMatch "^(config\.php|\.env|\.gitignore)$">
    Order deny,allow
    Deny from all
</FilesMatch>

# PHP налаштування
<IfModule mod_php.c>
    php_flag display_errors Off
    php_flag log_errors On
</IfModule>
